---
phase: 02-semantic-search-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/search/index.py
autonomous: true
requirements:
  - SRCH-02

must_haves:
  truths:
    - "Media descriptions can be indexed for semantic similarity search"
    - "Index supports efficient nearest neighbor queries"
  artifacts:
    - path: "src/search/index.py"
      provides: "Vector index for semantic similarity search using sklearn NearestNeighbors"
      min_lines: 60
      exports: ["VectorIndex", "build_index", "save_index", "load_index"]
  key_links:
    - from: "src/search/index.py"
      to: "src/search/embedder.py"
      via: "Embedder class import"
      pattern: "from src.search.embedder import"
    - from: "src/search/index.py"
      to: "sklearn.neighbors"
      via: "NearestNeighbors import"
      pattern: "from sklearn.neighbors import NearestNeighbors"
    - from: "src/search/index.py"
      to: "src/models/media.py"
      via: "MediaItem import"
      pattern: "from src.models.media import"
---

<objective>
Create the vector index module that builds and searches a semantic index over media descriptions.

Purpose: SRCH-02 requires indexing media descriptions for similarity search. The index enables fast nearest-neighbor queries using cosine similarity.
Output: VectorIndex class with build, search, save, and load methods.
</objective>

<execution_context>
@C:/Users/xiaobaima/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xiaobaima/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-search-core/02-RESEARCH.md
@.planning/phases/02-semantic-search-core/02-01-SUMMARY.md
@src/search/embedder.py
@src/models/media.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/search/embedder.py:
```python
class Embedder:
    def __init__(self, model_name: str = "all-MiniLM-L6-v2")
    def embed(self, text: str) -> np.ndarray  # Shape: (384,)
    def embed_batch(self, texts: List[str]) -> np.ndarray  # Shape: (n, 384)
```

From src/models/media.py:
```python
class MediaItem(BaseModel):
    id: str
    path: str
    description: str
    media_type: Literal["image", "video"]
    format: str
    categories: List[str] = []
    tags: List[str] = []

class MediaDataset(BaseModel):
    version: str = "1.0"
    items: List[MediaItem]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VectorIndex class for building and searching index</name>
  <files>src/search/index.py</files>
  <behavior>
    - Test: VectorIndex.build_index(dataset) creates NearestNeighbors index
    - Test: VectorIndex.search(query_embedding, top_k=5) returns indices and distances
    - Test: Distances are cosine distance (convert to similarity: 1 - distance)
    - Test: save_index() / load_index() persist and restore index
  </behavior>
  <action>
Create src/search/index.py with:
- VectorIndex class with:
  - __init__(embedder: Embedder) - takes embedder instance
  - build_index(dataset: MediaDataset) - generates embeddings for all descriptions, fits NearestNeighbors
  - search(query_embedding: np.ndarray, top_k: int) - returns (distances, indices)
  - save_index(path: str) - pickle index + embeddings + items to disk
  - load_index(path: str, embedder: Embedder) - classmethod to load persisted index

- Use sklearn.neighbors.NearestNeighbors with metric='cosine'
- Store items list for result construction
- Convert cosine distance to similarity: similarity = 1 - distance
- Handle edge cases: empty dataset, top_k > dataset size
  </action>
  <verify>
    <automated>python -c "
from src.search import Embedder
from src.search.index import VectorIndex
from src.models.media import MediaDataset

# Create test dataset
dataset = MediaDataset(items=[
    {'id': '1', 'path': 'a.jpg', 'description': 'A cat sleeping', 'media_type': 'image', 'format': 'jpg'},
    {'id': '2', 'path': 'b.jpg', 'description': 'A dog running', 'media_type': 'image', 'format': 'jpg'}
])

# Build and search
embedder = Embedder()
index = VectorIndex(embedder)
index.build_index(dataset)
distances, indices = index.search(embedder.embed('a sleeping pet'), top_k=2)
print(f'Search works: indices={indices.tolist()}, distances={distances.tolist()}')
"</automated>
  </verify>
  <done>VectorIndex can build index from dataset and search for nearest neighbors</done>
</task>

</tasks>

<verification>
- Index builds successfully from MediaDataset
- Search returns nearest neighbors with distances
- Index can be saved and loaded from disk
</verification>

<success_criteria>
- SRCH-02: Media descriptions indexed for semantic similarity search
- Uses cosine similarity for semantic matching
- Index persists for reuse without rebuilding
</success_criteria>

<output>
After completion, create .planning/phases/02-semantic-search-core/02-02-SUMMARY.md
</output>
