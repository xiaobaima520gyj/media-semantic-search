---
phase: 02-semantic-search-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/search/__init__.py
  - src/search/embedder.py
  - src/search/search_result.py
autonomous: true
requirements:
  - SRCH-01

must_haves:
  truths:
    - "User text prompts can be converted to vector embeddings"
    - "Embeddings capture semantic meaning, not just keywords"
  artifacts:
    - path: "src/search/embedder.py"
      provides: "Text-to-embedding conversion using sentence-transformers"
      min_lines: 40
      exports: ["Embedder", "DEFAULT_MODEL"]
    - path: "src/search/search_result.py"
      provides: "Search response models with relevance scoring"
      min_lines: 30
      exports: ["SearchResult", "SearchResponse"]
  key_links:
    - from: "src/search/embedder.py"
      to: "sentence-transformers"
      via: "SentenceTransformer import"
      pattern: "from sentence_transformers import"
    - from: "src/search/search_result.py"
      to: "pydantic"
      via: "BaseModel import"
      pattern: "from pydantic import BaseModel"
---

<objective>
Create the embedding module that converts user text prompts into vector embeddings, and define search result models for returning ranked results.

Purpose: This is the foundation for semantic search - without embeddings, similarity search is impossible. SRCH-01 requires converting text to vectors.
Output: Embedder class and SearchResult/SearchResponse Pydantic models.
</objective>

<execution_context>
@C:/Users/xiaobaima/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xiaobaima/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-semantic-search-core/02-RESEARCH.md
@src/models/media.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/models/media.py:
```python
class MediaItem(BaseModel):
    id: str
    path: str
    description: str
    media_type: Literal["image", "video"]
    format: str
    categories: List[str] = []
    tags: List[str] = []

class MediaDataset(BaseModel):
    version: str = "1.0"
    items: List[MediaItem]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Embedder module with sentence-transformers</name>
  <files>src/search/embedder.py, src/search/__init__.py</files>
  <behavior>
    - Test: embedder.embed("a sleeping cat") returns numpy array of shape (384,)
    - Test: embedder.embed_batch(["cat", "dog"]) returns array of shape (2, 384)
    - Test: Default model is "all-MiniLM-L6-v2"
  </behavior>
  <action>
Create src/search/embedder.py with:
- Embedder class wrapping sentence-transformers SentenceTransformer
- DEFAULT_MODEL = "all-MiniLM-L6-v2" constant
- embed(text: str) -> np.ndarray method for single text
- embed_batch(texts: List[str]) -> np.ndarray method for batch
- Store embedding_dim as property (384 for default model)

Create src/search/__init__.py to export Embedder and DEFAULT_MODEL.

Install dependencies: sentence-transformers, numpy, torch
  </action>
  <verify>
    <automated>python -c "from src.search import Embedder, DEFAULT_MODEL; e = Embedder(); print(e.embed('test').shape)"</automated>
  </verify>
  <done>Embedder can convert text to 384-dimensional embeddings</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SearchResult and SearchResponse models</name>
  <files>src/search/search_result.py</files>
  <behavior>
    - Test: SearchResult has id, path, description, media_type, format, score fields
    - Test: SearchResponse has query, results, total fields
    - Test: score is float between 0-1 (higher = more similar)
  </behavior>
  <action>
Create src/search/search_result.py with:
- SearchResult: Pydantic model with id, path, description, media_type, format, score
- SearchResponse: Pydantic model with query, results (List[SearchResult]), total
- Use float for score field (0-1 range, higher = more similar)
- Add field descriptions for documentation
  </action>
  <verify>
    <automated>python -c "from src.search.search_result import SearchResult, SearchResponse; r = SearchResult(id='1', path='a.jpg', description='test', media_type='image', format='jpg', score=0.95); print(r.model_dump())"</automated>
  </verify>
  <done>Search result models ready for returning ranked results</done>
</task>

</tasks>

<verification>
- Embedder class loads model and generates embeddings
- SearchResult/SearchResponse models validate correctly
- Module imports work from src.search package
</verification>

<success_criteria>
- SRCH-01: Text prompts converted to vector embeddings
- Embeddings are 384-dimensional (all-MiniLM-L6-v2)
- Search result models ready for API response
</success_criteria>

<output>
After completion, create .planning/phases/02-semantic-search-core/02-01-SUMMARY.md
</output>
